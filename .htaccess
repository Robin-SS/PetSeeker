DirectoryIndex index.php
RewriteEngine On

# Prevent directory listing
Options -Indexes

# Ensure assets and endpoints route cleanly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]