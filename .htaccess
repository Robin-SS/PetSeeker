DirectoryIndex index.php
RewriteEngine On

# Prevent direct directory listing for security
Options -Indexes

# If the request is for an existing physical file or directory, serve it directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Otherwise route requests through index.php
RewriteRule ^ index.php [L]